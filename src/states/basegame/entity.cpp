#include "entity.hpp"

namespace kme {
/*
Entity::Entity(Subworld& subworld,
               EntityID entity)
: subworld(subworld), entity(entity) {}

EntityID Entity::getID() const {
  return entity;
}

Entity Entity::spawnEntity(EntityType type) {
  Entity entity = subworld.spawnEntity(type);

  entity.set<Parent>(getID());
  entity.set<Position>(get<Position>());

  return entity;
}
*/
}
